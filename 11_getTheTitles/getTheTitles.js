const getTheTitles = function(arr) {
    // iterate through each item of the array
    // each item is an object
    // map item to object.title
    return arr.map((item) => item.title);
};

// Do not edit below this line
module.exports = getTheTitles;
